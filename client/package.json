{
  "name": "wbash-client",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel index.html --host client --port 8080",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "socket.io-client": "^4.5.0",
    "xterm": "^4.18.0"
  },
  "devDependencies": {
    "parcel": "^2.5.0"
  }
}
